extern crate library;

use library::PluginSystem;

fn main() {

    // Create the plugin system
    // Tell it to initialise
    let plugin_system = PluginSystem::new();

    // Start threads

    // Run some jobs

    // Stop it.

}
